import os
import sys
import envit
import utillib


def getcfvar(param):
	if not os.path.isfile(envir.CONF):
		return

	
def iscfvartrue(param):
	print param.split(' ')
	if getcfvar(param.split(' ')):
		#egrep staff

